import { AuthTabs } from "./_components/auth-tabs";

export const metadata = {
  title: "Entrar | PhotoCloud",
  description: "Entre ou cadastre-se no PhotoCloud",
};

export default function AuthPage() {
  return <AuthTabs />;
}
